APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# URL-prefix panelu admina (domyślnie "admin" → /admin).
# Zmień, aby ukryć panel pod innym adresem, np. ADMIN_PREFIX=zarzadzanie
# ADMIN_PREFIX=admin

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=redis
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Get a free Access Key at https://unsplash.com/developers (create an app,
# the "Demo" tier is enough — 50 requests/hour, no review needed).
UNSPLASH_ACCESS_KEY=

# Generowanie PDF (spatie/browsershot + headless Chromium).
# Ścieżka do binarki Chrome/Chromium — zostaw pustą, gdy Chromium jest w PATH.
# Na serwerach Linux bez sandboxu ustaw BROWSERSHOT_NO_SANDBOX=true.
BROWSERSHOT_CHROME_PATH=
BROWSERSHOT_NO_SANDBOX=false

# Logowanie do panelu przez Microsoft 365 (Laravel Socialite).
# Zarejestruj aplikację w Azure Portal → Microsoft Entra ID → App registrations.
# Redirect URI musi wskazywać na trasę callbacku, np. https://twojadomena.pl/auth/microsoft/callback
# MICROSOFT_TENANT_ID: "common" = dowolne konto; wpisz ID tenanta, aby ograniczyć do domeny org.
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_REDIRECT_URI="${APP_URL}/auth/microsoft/callback"
MICROSOFT_TENANT_ID=common

VITE_APP_NAME="${APP_NAME}"

# Blog "Wiem FEER" — osobna baza SQLite
BLOG_DB_DATABASE=

# Web Push (VAPID) — wygeneruj kluczem: php artisan tinker --execute="echo json_encode(Minishlink\WebPush\VAPID::createVapidKeys());"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
